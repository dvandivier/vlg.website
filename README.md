# vlg.website

Static site for **Vandivier Legacy Group LLC** — `vandivierlegacygroup.com`.

## Stack
- Static HTML / CSS / vanilla JS — no build step
- Hosted on Cloudflare Pages, deployed from `main` branch
- Domain at Namecheap, DNS on Cloudflare

## Local preview
Open `index.html` in a browser, or:

```bash
python3 -m http.server 8000
# then visit http://localhost:8000
```

## Brand reference
Brand spec lives in the workspace `b. references/brand/brief-30-04-2026.md` (in the agency repo, not this site repo).

## Status
v1 scaffold landed 2026-04-30 — homepage built, all other pages are coming-soon stubs that share the same nav + footer.
